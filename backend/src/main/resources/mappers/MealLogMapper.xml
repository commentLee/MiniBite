<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.minibite.dashboard.repository.MealLogMapper">

    <select id="nextId" resultType="int">
        SELECT COALESCE(MAX(log_id), 0) + 1 FROM meal_logs
    </select>

    <insert id="insertMealLog">
        INSERT INTO meal_logs (
            log_id,
            user_id,
            meal_time,
            photo_url,
            satisfaction_score,
            emotional_state,
            fullness_level,
            nausea,
            bloating
        ) VALUES (
            #{logId},
            #{userId},
            #{mealTime},
            #{photoUrl},
            #{satisfactionScore},
            #{emotionalState},
            #{fullnessLevel},
            #{nausea},
            #{bloating}
        )
    </insert>

</mapper>
